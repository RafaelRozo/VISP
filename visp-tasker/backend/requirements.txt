# =============================================================================
# VISP Backend -- Python Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# =============================================================================

# -- Core Framework --
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# -- Database --
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
alembic>=1.13.0
greenlet>=3.0.0

# -- Redis --
redis>=5.0.0

# -- Auth / JWT --
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# -- WebSocket / Real-time --
python-socketio>=5.10.0

# -- HTTP Client --
httpx>=0.26.0

# -- Payments --
stripe>=7.0.0

# -- Firebase Cloud Messaging --
firebase-admin>=6.2.0

# -- Background Jobs --
celery[redis]>=5.3.0

# -- Utilities --
python-multipart>=0.0.6
python-dotenv>=1.0.0
orjson>=3.9.0

# -- Testing --
pytest>=7.4.0
pytest-asyncio>=0.23.0
aiosqlite>=0.19.0
