# =============================================================================
# VISP Backend - Docker Compose
# =============================================================================
# Services: FastAPI Backend, PostgreSQL, Redis
# Run with: docker-compose up -d
# =============================================================================

services:
  # ---------------------------------------------------------------------------
  # FastAPI Backend
  # ---------------------------------------------------------------------------
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: visp-backend
    restart: always
    ports:
      - "8000:8000"
    environment:
      # Sin valor por defecto A PROPÓSITO. Antes traía clavada `visp_tasker`, que es
      # una base de junio: un despliegue que se apoyara en ese valor arrancaría contra
      # datos de hace meses sin dar ningún error. La base viva es `visp_prod` y la URL
      # tiene que venir del entorno o del .env del servidor.
      - DATABASE_URL=${DATABASE_URL:?falta DATABASE_URL (visp_prod) en el entorno}
      - REDIS_URL=redis://redis:6379/0
      - JWT_SECRET=${JWT_SECRET:-visp-dev-secret-change-me-in-production}
      - DEBUG=${DEBUG:-true}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY:-}
      - STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY:-}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET:-}
      - MAPBOX_ACCESS_TOKEN=${MAPBOX_ACCESS_TOKEN:-}
      - FIREBASE_CREDENTIALS_JSON=${FIREBASE_CREDENTIALS_JSON:-{}}
      - WS_CORS_ALLOWED_ORIGINS=${WS_CORS_ALLOWED_ORIGINS:-*}
    volumes:
      - ./src:/app/src
      - ./uploads:/app/uploads
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - visp-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # ---------------------------------------------------------------------------
  # Redis (for WebSocket and caching)
  # ---------------------------------------------------------------------------
  redis:
    image: redis:7-alpine
    container_name: visp-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - visp-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: redis-server --appendonly yes

# ---------------------------------------------------------------------------
# Networks
# ---------------------------------------------------------------------------
networks:
  visp-network:
    driver: bridge

# ---------------------------------------------------------------------------
# Volumes
# ---------------------------------------------------------------------------
volumes:
  redis_data:
